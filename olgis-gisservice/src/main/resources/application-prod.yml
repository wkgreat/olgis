server:
  port: 8080
  tomcat:
    max-threads: 200
    uri-encoding: utf-8
  address:

spring:

  thymeleaf:
    prefix: classpath:/static/
    suffix: .html
    mode: HTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false